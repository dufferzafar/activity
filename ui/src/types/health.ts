export interface HealthSummaryEntry {
  date: string; // ISO YYYY-MM-DD
  energy_burned_kcal: number; // calories burned
  steps: number; // steps taken
  walk_distance_meters: number; // distance walked
}


